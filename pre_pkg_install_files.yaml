write_files:
  - path: /root/script-enable-php-site
    permissions: "0755"
    owner: 'apache2'
    group: 'apache2'
    content: |
          #!/bin/bash
          echo "enable apache2 site"
          a2ensite yourdomain.conf;a2dissite 000-default.conf;systemctl restart apache2
          a2enmod dir;
    runcmd: echo "hello"


